import styled from 'styled-components';

import { sectionWrapperStyle } from '../../styleUtils';

export const Container: any = styled.div`
  ${sectionWrapperStyle}
  border-bottom: 1px solid black;
  text-align: center;
`;
